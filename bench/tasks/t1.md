Trace the full Socrata polling data pipeline in `app/api/jobs/poll/route.ts` end to end (this is the job Vercel Cron runs daily via vercel.json).

Cover every phase in order, with the exact code/functions involved:

1. SODA URL construction: how the `$where` watermark filter and `issued_date` field are derived, the pagination (limit), and the default watermark when `watermarkDatetime` is null.
2. Per-record normalization + quarantine: the exact conditions under which a raw record is quarantined into `quarantined_filings` (missing fields, invalid date, later no-coordinates), and what `errorLog` gets written.
3. Dedup against existing filings: the approach used and the exact SQL.
4. Concurrency-limited geocoding: which function limits concurrency, what timeout/bound exists.
5. Batch insert of new filings into `filings`, including how `geom` is built.
6. The batch PostGIS subscriber matcher: the ST_Contains query, how filing-type filtering works via `filing_type_filters`, and the NOT EXISTS dedup against `alerts_sent`.
7. Watermark + jurisdiction stats update at the end (which columns are written).

Also state: which tables are written in which phase, and where the report's `totalNewFilings` / `totalMatchedAlerts` counters are incremented.

Files you will likely need: `app/api/jobs/poll/route.ts`, `lib/db/schema.ts`, `migrations/001_critical_indexes.sql`, `vercel.json`. Do not change files.