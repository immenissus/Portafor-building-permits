Explain the daily digest job in `app/api/jobs/digest/route.ts` (Vercel Cron at 06:00 via vercel.json). This job turns `alerts_sent` rows into ONE email per subscriber.

Answer all of the following precisely:

1. The first query (subscribersWithAlerts): list the exact WHERE conditions (status, email, digested flag) and the join. What does `DISTINCT` protect against?
2. The per-subscriber permit query: what tables are joined, what columns are selected, and what is the ordering?
3. The "window" that defines which alerts make it into the digest: this committed version uses "start of today (UTC)" — describe exactly how `todayStart` is computed and what it means for alerts dispatched before midnight. Why would an alert dispatched at 23:59 be excluded even though it is `digested = false`?
4. Deduplication across runs: how the `digested` flag prevents the same alert from being emailed twice, and what happens on the NEXT run if sending failed (look at the try/catch per-subscriber behavior, and whether the flag is still flipped).
5. What gets written after a successful send, and what the report records for a failed send.

This is a read-only task. Do not change files.