Map the authentication / authorization architecture of the repo's API routes.

Read `lib/admin-auth.ts` and then survey the routes that use it. Answer all of the following:

1. Explain the difference between `verifyAdminKey` and `authorizeAdmin`. List the THREE credential paths `verifyAdminKey` accepts (header, bearer, cron secret) and in which order they are checked.
2. List which API routes call `authorizeAdmin` (e.g. /api/jobs/poll, /api/jobs/digest, /api/jobs/backfill, /api/jurisdictions) and which fall back to the Clerk-session admin check inside `authorizeAdmin`. What is the role-check that `authorizeAdmin` performs via `createClerkClient`?
3. `GET /api/filings` has its own auth: `X-Subscriber-Key` first (active subscriber), else `authorizeAdmin`. Explain why the subscriber-key path exists here and why it must NOT accept an admin key as a subscriber key.
4. `POST /api/billing/webhook` uses Stripe signature verification (`constructEvent`) instead of `authorizeAdmin`. Explain why that is the correct design for a webhook endpoint and what would be wrong about requiring an admin key there.
5. Security property: confirm from the code which environment variables must NEVER appear in a client bundle (`NEXT_PUBLIC_*`), and identify any place that would leak them.

Files to read: `lib/admin-auth.ts`, `app/api/filings/route.ts`, `app/api/jobs/poll/route.ts`, `app/api/billing/webhook/route.ts`, `app/api/jurisdictions/route.ts`. This is a read-only task. Do not change files.