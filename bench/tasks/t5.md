Read `lib/async.ts` and explain `mapLimit` precisely. This helper is used by the poll/backfill jobs to bound geocoding concurrency.

Answer all of the following:

1. Semantics: how it guarantees a bounded concurrency of `limit`, why results preserve input order, and how it behaves when one of the items rejects (does `Promise.all(workers)` reject fast or wait?).
2. Edge case A: `items = []`, any `limit`. What happens (workers array length, `Promise.all` result)?
3. Edge case B: `limit = 0` with a NON-empty `items` array. Walk through the code: `Math.min(0, items.length)` yields how many workers? What does the returned array contain (sparse array with holes)? Does the function hang, resolve, or reject? Is this a bug, and if so, is it reachable from the current callers (`app/api/jobs/poll/route.ts`, `app/api/jobs/backfill/route.ts`)?
4. Edge case C: `limit` larger than `items.length`. Confirm the behavior.
5. Edge case D: a single item rejects. Does `Promise.all` reject? Does any other worker keep running?

Give a concrete trace for the `limit = 0` case. This is a read-only task. Do not change files.