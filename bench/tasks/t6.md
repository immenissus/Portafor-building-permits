Analyze the spatial proximity query in `app/api/filings/route.ts` together with the index definitions in `migrations/002_geography_indexes.sql`.

Answer all of the following:

1. Index usage: explain why `ST_DWithin(geom::geography, point::geography, radius)` can use an index while `ST_DistanceSphere(geom, point)` cannot. Name the exact index (from migrations/002) that backs the WHERE clause, and what expression it is created on.
2. Output distance: the query still computes `ST_DistanceSphere(geom, searchPoint) AS distance_meters` for ordering/display. Why is it fine to use `ST_DistanceSphere` here when the WHERE clause correctly avoids it? What does the ORDER BY use?
3. Coordinate plumbing: trace how `near` is parsed, how `searchPoint` is built (`ST_SetSRID(ST_MakePoint(longitude, latitude), 4326)`), and why longitude is passed before latitude to ST_MakePoint.
4. Radius semantics: `radius_km` default (5.0), the `radiusKm * 1000.0` conversion for geography meters, and the interaction with ST_DWithin's geography-based distance (meters on a sphere).
5. Failure mode: what happens to this query (performance AND correctness) if `migrations/002_geography_indexes.sql` was never applied to the database? Explain in terms of full-table scans.

Files to read: `app/api/filings/route.ts`, `migrations/002_geography_indexes.sql`, `migrations/001_critical_indexes.sql`. This is a read-only task. Do not change files.