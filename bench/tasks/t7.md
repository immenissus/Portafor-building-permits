Trace the Stripe billing webhook pipeline in `app/api/billing/webhook/route.ts` and the "self-healing queue" in `app/api/jobs/poll/route.ts`.

Answer all of the following:

1. Signature verification: how `stripe.webhooks.constructEvent` validates the request, and what is returned on failure (status + body).
2. Event dedup: how `stripeWebhookEvents.id` is used to avoid double-processing, and what the code does when an already-processed event arrives.
3. Status machine: the meaning of `pending` / `processed` / `failed` on `stripeWebhookEvents`, and where each transition happens (insert as pending, update to processed/failed after the switch).
4. `clerkUserId` resolution: the two paths — `checkout.session.completed` uses `client_reference_id`/`metadata.clerk_user_id`; other subscription events look up the Stripe customer and read `customer.metadata.clerk_user_id`. Explain why the customer lookup exists.
5. What each handled event type does to Clerk `publicMetadata` (checkout.session.completed, customer.subscription.created/updated, invoice.paid, invoice.payment_failed, customer.subscription.deleted) — list the exact plan/status values written.
6. The self-healing queue in `app/api/jobs/poll/route.ts` (top of GET): which events it rescues, what it does for `checkout.session.completed`/`invoice.paid` vs `customer.subscription.deleted`, and how it marks them processed.
7. Correctness concern: identify the potential problem with the self-healing queue writing hardcoded plan names (e.g. "Starter Yearly") into `publicMetadata`, especially for a user who might have a different plan or a `role: admin` in their metadata. Does `users.updateUser({ publicMetadata: { plan, status } })` merge or replace?

Files to read: `app/api/billing/webhook/route.ts`, `app/api/jobs/poll/route.ts`, `lib/db/schema.ts`. This is a read-only task. Do not change files.